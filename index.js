function change_mountain(mountain){
    // I didn't have time to write these functions
    // What I would do is use the visible and invisible class to hide all current sections
    // Then I would use the passed argument to add the visible and relevant css tags to their respective elements.
}

function change_carousel(slide_number){
    // I didn't have time to write these functions
    // What I would do is use the visible and invisible class to hide all current sections
    // Then I would use the passed argument to add the visible and relevant css tags to their respective elements.
}